VITE_API_URL=http://localhost:4000/api

# Optional — used to build shareable links. Defaults to wherever the app
# is currently running if left unset (fine for local development).
VITE_SHARE_BASE=http://localhost:5173
